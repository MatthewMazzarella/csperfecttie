<%= form_for(homeone) do |f| %>
  <% if homeone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(homeone.errors.count, "error") %> prohibited this homeone from being saved:</h2>

      <ul>
      <% homeone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
